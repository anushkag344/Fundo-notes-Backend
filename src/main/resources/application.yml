server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: fundoonotes

  # ---------- POSTGRESQL ----------
  datasource:
    url: ${DB_URL:jdbc:postgresql://ep-quiet-band-aygcmxkj.c-5.us-east-2.aws.neon.tech/neondb?sslmode=require}
    username: ${DB_USERNAME:neondb_owner}
    password: ${DB_PASSWORD:npg_bTHutrnRKe37}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  # ---------- REDIS ----------
  data:
    redis:
      host: ${REDIS_HOST:desired-tahr-151099.upstash.io}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:gQAAAAAAAk47AAIgcDJlOGZlNzVjODJhNTk0OTk5YjAwNGYzZTlmNDNhMzAwYQ}

  # ---------- RABBITMQ ----------
  rabbitmq:
    addresses: ${RABBITMQ_URL:}
    host: ${RABBITMQ_HOST:puffin.rmq2.cloudamqp.com}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:mgtwtxz}
    password: ${RABBITMQ_PASSWORD:qjZ7MvkErzT4f7TiCA0oOuNSvyTbuPpI}
    virtual-host: ${RABBITMQ_VHOST:mgtwtxz}
    ssl:
      enabled: ${RABBITMQ_SSL_ENABLED:true}
    connection-timeout: 5000ms

  # ---------- MAIL (for OTP & Reminders) ----------
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:anushkag344@gmail.com}
    password: ${MAIL_PASSWORD:nesmbccvdagjknhw}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: "*"
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

  # ---------- GOOGLE OAUTH2 ----------
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:77956562772-naaalloqkofkpn41fc5u75puunmm0a8k.apps.googleusercontent.com}
            client-secret: ${GOOGLE_CLIENT_SECRET:dummy_secret}
            scope:
              - email
              - profile

# ---------- JWT ----------
jwt:
  secret: ${JWT_SECRET:7fd52ad8b81379ded4daa446bcfccde9442aaa41bd40bc64c17f5348a6a1cea7}
  expiration: ${JWT_EXPIRATION:86400000}

# ---------- APP-SPECIFIC ----------
app:
  oauth2:
    redirect-uri: ${OAUTH2_REDIRECT_URI:https://fundo-notes-frontend.vercel.app/login}

# ---------- SWAGGER / OPENAPI ----------
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
